[tool.poetry]
name = "multi-split-decision-tree"
version = "0.1.0"
description = ""
authors = ["Mikhail Martin <johndoe110@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
pandas = "^2.1.4"
scikit-learn = "^1.3.2"
graphviz = "^0.20.1"

[tool.poetry.group.dev.dependencies]
notebook = "^7.0.6"
jupyterlab-execute-time = "^3.1.0"
pytest = "^7.4.3"

[tool.poetry.group.test.dependencies]
pytest = "^7.4.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
